<div data-controller="collapsible">
  <button type="button" data-collapsible-target="trigger" data-action="click->collapsible#toggle"
    <%# w-full to match Accordion's trigger: a bare <button> shrinks to its
        content, so a trigger laying its slot out with justify-between had
        nothing to justify against and the row collapsed to the left.
        min-h-9: a one-line label made a 21px target (the Menu's day rows),
        under the 36px floor; flex + items-center keeps it vertically centred. %>
    aria-expanded="<%= @open %>" aria-controls="<%= panel_id %>" class="group flex min-h-9 w-full items-center">
    <%= trigger %>
  </button>
  <div id="<%= panel_id %>" data-collapsible-target="panel" <%= "hidden" unless @open %> data-state="<%= @open ? "open" : "closed" %>"
    class="overflow-hidden data-[state=open]:animate-accordion-down data-[state=closed]:animate-accordion-up">
    <div class="pt-2"><%= content %></div>
  </div>
</div>
